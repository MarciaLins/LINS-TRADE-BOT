import express from "express";

const app = express();
const PORT = process.env.PORT || 8080;

app.get("/", (req, res) => {
  res.send("🤖 Lins Trade Bot ativo e rodando no Railway!");
});

app.listen(PORT, () => {
  console.log(Servidor do Lins Trade rodando na porta ${PORT});
});
